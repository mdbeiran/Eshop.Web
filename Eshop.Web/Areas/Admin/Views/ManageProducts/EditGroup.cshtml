@model Eshop.DomainClass.Product.ProductGroup
@{
    ViewBag.Title = "ویرایش گروه";
}

<!-- Page Breadcrumb -->
<div class="page-breadcrumbs">
    <ul class="breadcrumb">
        <li>
            <i class="fa fa-home"></i>
            <a href="#">خانه</a>
        </li>
        <li class="active">مدیریت محصولات</li>
        <li class="active">ویرایش گروه</li>
    </ul>
</div>
<!-- /Page Breadcrumb -->
<!-- Page Header -->
<div class="page-header position-relative">
    <div class="header-title">
        <h1>
            ویرایش
        </h1>
    </div>
    <!--Header Buttons-->
    <div class="header-buttons">
        <a class="sidebar-toggler" href="#">
            <i class="fa fa-arrows-h"></i>
        </a>
        <a class="refresh" id="refresh-toggler" href="">
            <i class="glyphicon glyphicon-refresh"></i>
        </a>
        <a class="fullscreen" id="fullscreen-toggler" href="#">
            <i class="glyphicon glyphicon-fullscreen"></i>
        </a>
    </div>
    <!--Header Buttons End-->
</div>
<!-- /Page Header -->
<!-- Page Body -->
<div class="page-body">
    <div class="row">
        <div class="col-lg-12 col-sm-12 col-xs-12">
            @if (ViewBag.UrlError)
            {
                <div class="alert alert-warning">
                    عنوان لینک از قبل وجود دارد
                </div>
            }
            @using (Html.BeginForm("EditGroup", "ManageProducts", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.ParentId)
                @Html.HiddenFor(m => m.GroupId)
                <input type="text" value="@Model.NameInUrl" name="oldUrlName" hidden />
                <div class="row">
                    <div class="col-md-12">
                        <div class="widget flat radius-bordered">
                            <div class="widget-header bg-yellow">
                                <span class="widget-caption">ویرایش گروه</span>
                            </div>
                            <div class="widget-body">
                                <div id="registration-form">
                                    <div class="form-group col-md-4">
                                        <span class="input-icon icon-right">
                                            @Html.EditorFor(model => model.GroupTitle, new { htmlAttributes = new { @class = "form-control", PlaceHolder = Html.DisplayNameFor(m => m.GroupTitle) } })
                                            @Html.ValidationMessageFor(model => model.GroupTitle, "", new { @class = "text-danger" })
                                            <i class="fa fa-group circular"></i>
                                        </span>
                                    </div>
                                    <div class="form-group col-md-4">
                                        <span class="input-icon icon-right">
                                            @Html.EditorFor(model => model.NameInUrl, new { htmlAttributes = new { @class = "form-control", PlaceHolder = Html.DisplayNameFor(m => m.NameInUrl) } })
                                            @Html.ValidationMessageFor(model => model.NameInUrl, "", new { @class = "text-danger" })
                                            <i class="fa fa-link circular"></i>
                                        </span>
                                    </div>
                                    <div class="form-group col-md-4">
                                        <button type="submit" class="btn btn-warning btn-block">ویرایش گروه</button>
                                    </div>
                                    <div class="clearfix"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>